"""
Hermes Bridge API — REST + SSE bridge for Agentfy and other mobile/device clients.

A pluggable FastAPI server that exposes Hermes agents via simple HTTP endpoints.
No OpenClaw protocol needed — plain REST + SSE that any HTTP client can use.
"""

__version__ = "0.1.0"
